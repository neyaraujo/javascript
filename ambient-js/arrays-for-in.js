// Declaração de um array com valores numéricos
let valores = [8, 1, 7, 4, 2, 9]

// Estrutura for...in para iterar pelas posições do array
for (let pos in valores) {
    console.log(`A posição ${pos} tem o valor ${valores[pos]}`)
}

